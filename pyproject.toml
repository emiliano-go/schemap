[project]
name = "schemap"
version = "0.6.1"
description = "Automatic Pydantic Schemas for SQLAlchemy models"
readme = "README.md"
requires-python = ">=3.12.7"
authors = [
    { name = "Emiliano Gandini Outeda", email = "emiliano.gandini@protonmail.com" }
]
license = "MIT"
keywords = [
  "python",
  "schemas",
  "models",
  "orm"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries",
]

dependencies = [
    "cached-classproperty>=1.1.0",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.49",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]
docs = ["seoslug>=2.0.1", "zensical"]

[project.urls]
Homepage = "https://github.com/emiliano-go/schemap"
Repository = "https://github.com/emiliano-go/schemap"
Issues = "https://github.com/emiliano-go/schemap/issues"
Documentation = "https://schemap.emiliano-go.com"
Changelog = "https://github.com/emiliano-go/schemap/releases"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests*", "examples*"]

[tool.setuptools.package-data]
schemap = ["py.typed"]
